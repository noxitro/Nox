<UserControl x:Class="Core.UI.Views.ProjectSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:vm="clr-namespace:Core.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="980"
             d:DataContext="{d:DesignInstance vm:ProjectSettingsViewModel, IsDesignTimeCreatable=False}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             AutomationProperties.AutomationId="NoxStudio.ProjectSettingsView">
    <Border Background="{DynamicResource Nox.Brush.SurfaceBackground}" BorderBrush="{DynamicResource Nox.Brush.GridLine}" BorderThickness="1">
        <DockPanel>
            <Border DockPanel.Dock="Top" Padding="10" Background="{DynamicResource Nox.Brush.HeaderBackground}" BorderBrush="{DynamicResource Nox.Brush.GridLine}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Project Settings" Foreground="{DynamicResource Nox.Brush.TextHeading}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource Nox.Brush.TextTertiary}" Margin="10,0,0,0" />
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Save"
                            AutomationProperties.AutomationId="NoxStudio.ProjectSettings.SaveButton"
                            Height="28"
                            MinWidth="80"
                            Command="{Binding SaveCommand}" />
                </Grid>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Groups}" Margin="12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ProjectSettingsGroupViewModel}">
                            <Border Margin="0,0,0,12" Padding="12" Background="{DynamicResource Nox.Brush.SurfaceRaised}" BorderBrush="{DynamicResource Nox.Brush.GridLine}" BorderThickness="1" CornerRadius="6">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Category}" Foreground="{DynamicResource Nox.Brush.Accent}" FontWeight="SemiBold" />
                                        <TextBlock Text=" / " Foreground="{DynamicResource Nox.Brush.TextMuted}" />
                                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource Nox.Brush.TextHeading}" FontWeight="SemiBold" />
                                    </StackPanel>

                                    <ItemsControl ItemsSource="{Binding Properties}" Margin="0,10,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:ProjectSettingsPropertyViewModel}">
                                                <Grid Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="220" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="120" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="{DynamicResource Nox.Brush.TextPrimary}" VerticalAlignment="Center" />

                                                    <TextBox Grid.Column="1"
                                                             Height="28"
                                                             VerticalContentAlignment="Center"
                                                             Text="{Binding ValueText, UpdateSourceTrigger=PropertyChanged}">
                                                        <TextBox.Style>
                                                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsText}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBox.Style>
                                                    </TextBox>

                                                    <CheckBox Grid.Column="1"
                                                              VerticalAlignment="Center"
                                                              IsChecked="{Binding BoolValue, Mode=TwoWay}">
                                                        <CheckBox.Style>
                                                            <Style TargetType="CheckBox">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Setter Property="Foreground" Value="{DynamicResource Nox.Brush.TextHeading}" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsBoolean}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </CheckBox.Style>
                                                    </CheckBox>

                                                    <ComboBox Grid.Column="1"
                                                              Height="28"
                                                              ItemsSource="{Binding EnumValues}"
                                                              SelectedItem="{Binding SelectedEnumValue, Mode=TwoWay}">
                                                        <ComboBox.Style>
                                                            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsEnum}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </ComboBox.Style>
                                                    </ComboBox>

                                                    <TextBlock Grid.Column="2" Text="{Binding TypeName}" Foreground="{DynamicResource Nox.Brush.TextMuted}" Margin="8,0,0,0" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ErrorText}" Foreground="{DynamicResource Nox.Brush.ErrorText}" Margin="0,3,0,0">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>

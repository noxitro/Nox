<UserControl x:Class="Core.UI.Views.RuntimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Core.UI.Views"
             xmlns:noxui="clr-namespace:NoxUI;assembly=NoxUI"
             xmlns:vm="clr-namespace:Core.UI.ViewModels"
             noxui:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:RuntimeViewModel, IsDesignTimeCreatable=True}"
             >
    <Grid Background="{DynamicResource Nox.Brush.PanelBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Runtime View"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontWeight="Bold"
                   AutomationProperties.AutomationId="NoxStudio.RuntimeView.AttachStatus"
                   AutomationProperties.Name="{Binding IsAttached, ElementName=RuntimeSceneView}">
            <AutomationProperties.HelpText>
                <MultiBinding StringFormat="AttachError={0}; {1}">
                    <Binding ElementName="RuntimeSceneView" Path="AttachError"/>
                    <Binding Path="DebugStatus"/>
                </MultiBinding>
            </AutomationProperties.HelpText>
        </TextBlock>
        <!--SceneViewの配置-->
        <local:RutnimeSceneView 
            
             x:Name="RuntimeSceneView"
             Grid.Row="1"
              WindowHandle="{Binding MainWindowHandle}"
            
              >

        </local:RutnimeSceneView>

    </Grid>
</UserControl>

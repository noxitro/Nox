<UserControl x:Class="Core.UI.Views.RuntimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Core.UI.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:vm="clr-namespace:Core.UI.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:RuntimeViewModel, IsDesignTimeCreatable=True}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Runtime View" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>

        <!--SceneViewの配置-->
        <local:RutnimeSceneView 
            
            x:Name="RuntimeSceneView"
            Grid.Row="1"
            SceneView="{Binding MainView}"
            >

        </local:RutnimeSceneView>

    </Grid>
</UserControl>

<UserControl x:Class="Core.UI.Views.EngineSystemGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:noxui="clr-namespace:NoxUI;assembly=NoxUI"
             xmlns:views="clr-namespace:Core.UI.Views"
             xmlns:vm="clr-namespace:Core.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="900"
             d:DataContext="{d:DesignInstance vm:EngineSystemGraphViewModel, IsDesignTimeCreatable=False}"
             noxui:ViewModelLocator.AutoWireViewModel="True"
             AutomationProperties.AutomationId="NoxStudio.EngineSystemGraphView">
    <Border Background="{DynamicResource Nox.Brush.SurfaceBackground}"
            BorderBrush="{DynamicResource Nox.Brush.GridLine}"
            BorderThickness="1">
        <DockPanel>
            <Border DockPanel.Dock="Top"
                    Padding="8"
                    Background="{DynamicResource Nox.Brush.HeaderBackground}"
                    BorderBrush="{DynamicResource Nox.Brush.GridLine}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="EngineSystem Dependency Graph"
                                   Foreground="{DynamicResource Nox.Brush.TextHeading}"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="{Binding StatusText}"
                                   Foreground="{DynamicResource Nox.Brush.TextMuted}"
                                   FontSize="11" />
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Refresh"
                            MinWidth="84"
                            Padding="8,3"
                            Command="{Binding RefreshCommand}" />
                </Grid>
            </Border>

            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Background="{DynamicResource Nox.Brush.SurfaceBackground}">
                <views:PropertyInspectorView ItemsSource="{Binding GraphFields}"
                                             LabelWidth="150"
                                             PropertyInspectorAutomationId="NoxStudio.EngineSystemGraph.Fields" />
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
